name: crane-postgres-nio-integration-tests
services:
  postgres:
    image: postgres:18
    environment:
      - POSTGRES_USER=integration
      - POSTGRES_PASSWORD=integration
    ports:
      - 5432:5432
